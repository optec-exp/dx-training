# 作品21 — 汇率显示应用（USD/EUR/CNY↔JPY）

| | |
|---|---|
| **编号** | 21 |
| **工时** | M（基础）半天 |
| **Day** | Day 16 |
| **对象** | 全员 |

## 要做什么

制作从外部汇率 API 获取实时汇率，并支持 USD/EUR/CNY 与 JPY 互换计算的应用。

## 学习内容

- **`fetch` / `async-await`** — 首次调用外部 API
- 从 API 获取 JSON 数据并渲染
- 环境变量管理 API Key（`.env.local`）
- 加载中状态显示

## 完成标准

- [ ] 显示当前实时汇率
- [ ] 输入金额后自动换算结果
- [ ] API Key 通过环境变量注入（不硬编码）
- [ ] Vercel 部署成功

## 提交方式

```
git add .
git commit -m "作品21: 汇率显示应用（USD/EUR/CNY↔JPY）"
git push
```

Vercel 自动部署后，将公开 URL 发送到 **#optec-ai-academy**。
