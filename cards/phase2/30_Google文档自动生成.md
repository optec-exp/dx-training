# 作品30 — Google 文档自动生成（模板＋数据填充 → PDF）

| | |
|---|---|
| **编号** | 30 |
| **工时** | M（基础）半天 |
| **Day** | Day 24 |
| **对象** | 全员 |

## 要做什么

制作根据模板 Google 文档和输入数据自动生成新文档并导出 PDF 的工具。

## 学习内容

- Google Docs API（复制模板 + 批量替换占位符）
- Google Drive API（权限设置）
- 文档自动生成流程

## 完成标准

- [ ] 输入数据后自动生成文档
- [ ] 占位符（如 `{{客户名}}`）被正确替换
- [ ] 可导出/下载 PDF
- [ ] Vercel 部署成功

## 提交方式

```
git add .
git commit -m "作品30: Google 文档自动生成（模板＋数据填充 → PDF）"
git push
```

Vercel 自动部署后，将公开 URL 发送到 **#optec-ai-academy**。
