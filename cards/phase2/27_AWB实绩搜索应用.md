# 作品27 — AWB 实绩搜索应用（按期间・客户・状态筛选）

| | |
|---|---|
| **编号** | 27 |
| **工时** | M（基础）半天 |
| **Day** | Day 19 |
| **对象** | 全员 |

## 要做什么

制作可按期间、客户、状态多条件筛选 AWB 实绩数据的搜索应用，支持 CSV 导出。

## 学习内容

- 复合搜索（多条件 AND 查询）
- Kintone 查询语法（`query` 参数）
- CSV 导出（`Blob` + `URL.createObjectURL`）

## 完成标准

- [ ] 期间、客户、状态三个条件可组合筛选
- [ ] 「导出 CSV」按钮可下载筛选结果
- [ ] Vercel 部署成功

## 提交方式

```
git add .
git commit -m "作品27: AWB 实绩搜索应用（按期间・客户・状态筛选）"
git push
```

Vercel 自动部署后，将公开 URL 发送到 **#optec-ai-academy**。
